--  This package has been generated automatically by GNATtest.
--  You are allowed to add your code to the bodies of test routines.
--  Such changes will be kept during further regeneration of this file.
--  All code placed outside of test routine bodies will be lost. The
--  code intended to set up and tear down the test environment should be
--  placed into Mozzoni.Commands.Test_Data.

with AUnit.Assertions; use AUnit.Assertions;
with System.Assertions;

--  begin read only
--  id:2.2/00/
--
--  This section can be used to add with clauses if necessary.
--
--  end read only

--  begin read only
--  end read only
package body Mozzoni.Commands.Test_Data.Tests is

--  begin read only
--  id:2.2/01/
--
--  This section can be used to add global variables and other elements.
--
--  end read only

--  begin read only
--  end read only

--  begin read only
   procedure Test_1_Prepare_Response (Gnattest_T : in out Test);
   procedure Test_Prepare_Response_81ad58 (Gnattest_T : in out Test) renames Test_1_Prepare_Response;
--  id:2.2/81ad58308148dae2/Prepare_Response/1/0/
   procedure Test_1_Prepare_Response (Gnattest_T : in out Test) is
   --  mozzoni-commands.ads:9:4:Prepare_Response
--  end read only

      pragma Unreferenced (Gnattest_T);

   begin

      AUnit.Assertions.Assert
        (Gnattest_Generated.Default_Assert_Value,
         "Test not implemented.");

--  begin read only
   end Test_1_Prepare_Response;
--  end read only


--  begin read only
   procedure Test_2_Prepare_Response (Gnattest_T : in out Test);
   procedure Test_Prepare_Response_7a26ca (Gnattest_T : in out Test) renames Test_2_Prepare_Response;
--  id:2.2/7a26cad728daf099/Prepare_Response/0/0/
   procedure Test_2_Prepare_Response (Gnattest_T : in out Test) is
   --  mozzoni-commands.ads:11:4:Prepare_Response
--  end read only

      pragma Unreferenced (Gnattest_T);

   begin

      AUnit.Assertions.Assert
        (Gnattest_Generated.Default_Assert_Value,
         "Test not implemented.");

--  begin read only
   end Test_2_Prepare_Response;
--  end read only


--  begin read only
   procedure Test_Handle_Ping (Gnattest_T : in out Test);
   procedure Test_Handle_Ping_9b89ba (Gnattest_T : in out Test) renames Test_Handle_Ping;
--  id:2.2/9b89baf367524907/Handle_Ping/1/0/
   procedure Test_Handle_Ping (Gnattest_T : in out Test) is
   --  mozzoni-commands.ads:14:4:Handle_Ping
--  end read only

      pragma Unreferenced (Gnattest_T);

   begin

      AUnit.Assertions.Assert
        (Gnattest_Generated.Default_Assert_Value,
         "Test not implemented.");

--  begin read only
   end Test_Handle_Ping;
--  end read only

--  begin read only
--  id:2.2/02/
--
--  This section can be used to add elaboration code for the global state.
--
begin
--  end read only
   null;
--  begin read only
--  end read only
end Mozzoni.Commands.Test_Data.Tests;
